@{
    Layout = "~/Views/Shared/_WindowLayout.cshtml";
}
@using Vino.Core.CMS.Domain.Dto.Membership
@model MemberDto
<div class="window-content">
    @using (Html.LayuiBeginForm<MemberDto>("Save"))
    {
        @Html.LayuiInputFor(x => x.Id)
        @Html.LayuiInputFor(x => x.Name)
        @Html.LayuiInputFor(x => x.Password)
        @Html.LayuiInputFor(x => x.Mobile)
        @Html.LayuiInputFor(x => x.Sex)
        @Html.LayuiInputFor(x => x.IsEnable)

        <div class="layui-form-item">
            <label class="layui-form-label">会员类型</label>
            <div class="layui-input-block">
                <select name="MemberTypeId" lay-verify="required">
                    <option value="">请选择会员类型</option>
                    @foreach (MemberTypeDto memberType in ViewBag.MemberTypeList)
                    {
                        if (memberType.Id == Model.MemberTypeId)
                        {
                            <option value="@memberType.Id" selected="selected">@memberType.Name</option>
                        }
                        else
                        {
                            <option value="@memberType.Id">@memberType.Name</option>
                        }
                    }
                </select>
            </div>
        </div>

        @Html.LayuiFormActionsForSubmitAndClose()
    }
</div>