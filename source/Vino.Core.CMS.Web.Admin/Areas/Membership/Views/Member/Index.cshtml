@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="main-content">
    <div class="toolbar">
        <action auth-code="membership.member.edit" text="添加" size="small" icon="&#xe608;" action="window:Membership/Member/Edit" after="gridReload()"/>
        <action text="刷新" size="small" icon="&#x1002;" action="javascript:gridReload()" />
    </div>
    <table id="list">
    </table>
    <div id="pager"></div>
</div>
@section Scripts
{
    <script>
        layui.use(['layer'],
            function() {
                var $ = layui.jquery, layer = layui.layer;
            });

        $(function() {
            $("#list").vinoGrid({
                url: 'GetList',
                colModel: [
                    { label: '姓名', name: 'Name', width: 75 },
                    { label: '手机', name: 'Mobile', width: 100 },
                    { label: '状态', name: 'IsEnable', width: 40, formatter: gridFormatter_Is },
                    { label: '会员类型', name: 'MemberType.Name', width: 50 },
                    { label: '积分', name: 'Points', width: 40 },
                    { label: '等级', name: 'Level', width: 40 },
                    { label: '创建时间', name: 'CreateTime', width: 100 },
                    { label: '登陆时间', name: 'LastLoginTime', width: 100 },
                    {
                        label: '操作', name: 'Id', width: 100
                        , formatter: function(value, options, row) {
                            return gridFormatter_Operate([
                                { code: "membership.member.edit", title: "编辑", action: `window:Membership/Member/Edit?id=${row.Id}` },
                                { code: "membership.member.delete", title: "删除", action: `post:Delete?id=${row.Id}` }
                            ]);
                        }
                    }
                ],
                pager: "#pager"
            });
        });
    </script>
}
